# 来财 (Attract-wealth) 依赖锁文件
# 由 pyproject.toml 直接依赖 + 当前环境实际版本生成。
# 锁定版本以保证可重现构建；升级依赖后请重新生成（pip freeze 对照 pyproject.toml）。

aiosqlite==0.21.0
akshare==1.18.54
APScheduler==3.11.2
fastapi==0.116.1
httpx==0.28.1
langchain-core==1.2.28
langgraph==1.1.6
openai==2.30.0
passlib==1.7.4
pillow==11.3.0
PyAutoGUI==0.9.54
pydantic==2.12.5
pydantic-settings==2.10.1
pytest==8.4.2
pytest-asyncio==1.2.0
python-dotenv==1.1.1
python-jose==3.5.0
pywin32==311
pywinauto==0.6.9
redis==6.4.0
rich==13.9.4
toml==0.10.2
uvicorn==0.35.0

# 以下直接依赖在当前环境未安装（可能属于可选组，如 xtquant 需手动安装）：
#   baostock
#   lancedb
#   pre-commit
#   ruff
#   tushare
